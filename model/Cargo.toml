[package]
name = "lumberjack-model"
version = "1.0.0"
edition = "2024"

[dependencies]
aligned-vec = { version = "0.6.1", optional = true }
half = { workspace = true }
heapless = "0.9.3"
zerocopy = { workspace = true }

[dev-dependencies]
anyhow = "1.0.91"

[features]
std = ["dep:aligned-vec"]
